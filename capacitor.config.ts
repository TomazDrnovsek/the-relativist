import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'si.tomazdrnovsek.relativist',
  appName: 'The Relativist',
  webDir: 'dist'
};

export default config;
